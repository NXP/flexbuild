#!/bin/bash

# Copyright 2017-2025 NXP
#
# SPDX-License-Identifier: BSD-3-Clause


build_distro_rfs_debian() {
    [ $DISTROTYPE = debian -a -f $RFSDIR/etc/buildinfo ] && log_mute echo $RFSDIR already exists! && exit 0
    [ `whoami` = root ] && sudopt="" || sudopt=sudo

	mkdir -p $RFSDIR
    fbprint_n "Generating initial rootfs: $RFSDIR ... " && cd $FBDIR
    test -c $RFSDIR/dev/pts/ptmx && $sudopt umount $RFSDIR/dev/pts
    test -f $RFSDIR/proc/uptime && $sudopt umount $RFSDIR/proc
    [ -n "$RFSDIR" ] && $sudopt rm -rf $RFSDIR/*

    if [ "$FETCH_PREBUILT_SDK_RFS" = y -a "$FORCE" != y ]; then
	echo FETCH_PREBUILT_SDK_RFS is enable by default in $CFGLISTYML
	rootfsn=rootfs_${DISTRIB_VERSION}_${DISTROTYPE}_${DISTROVARIANT}_${DESTARCH}.tar.zst
	rootfs_url=$DISTRO_SVR_URL/$DISTRIB_VERSION/$rootfsn
	if [ ! -f $FBOUTDIR/images/$rootfsn ]; then
	   cd $FBOUTDIR/images
	   log_mute wget --progress=bar:force $rootfs_url || true
	   cd -
	fi
	[ -f $FBOUTDIR/images/$rootfsn ] && echo Extracting $rootfsn && \
	$sudopt tar -I zstd -xf $FBOUTDIR/images/$rootfsn -C $RFSDIR && \
	fbprint_d $RFSDIR && exit || echo Building $RFSDIR from scratch
    fi

    if [ ! -f $PKGDIR/${DEBIAN_CODENAME}_${DISTROVARIANT}_$DESTARCH/etc/buildinfo ]; then
		show_one_line $sudopt bash $FBDIR/configs/debian/debian_${DISTROVARIANT}_$DESTARCH.sh $PKGDIR/${DEBIAN_CODENAME}_${DISTROVARIANT}_$DESTARCH
    fi

    [ -f $RFSDIR/etc/buildinfo ] || $sudopt rsync -aHAX $PKGDIR/${DEBIAN_CODENAME}_${DISTROVARIANT}_$DESTARCH/* $RFSDIR

    if [ $DISTROVARIANT = server -o $DISTROVARIANT = desktop ]; then
		mount -t proc /proc "$RFSDIR/proc"
		mount -t sysfs /sys "$RFSDIR/sys"
		mount --bind /dev "$RFSDIR/dev"
		mount --bind /dev/pts "$RFSDIR/dev/pts"

		show_one_line $sudopt chroot $RFSDIR apt-get update
		case "$MACHINE" in
			   imx8*|ls1028*)   PKGS="$ls_pkgs $imx9_pkgs $imx8_pkgs"; log_mute echo imx8 package...  ;;
			   imx9*)   PKGS="$ls_pkgs $imx9_pkgs" ; log_mute echo imx9 package...... ;;
			   ls*|lx*) PKGS="$ls_pkgs" ; log_mute echo ls  package ... ;;
			   *)       echo "machine is not supported"; exit 1;  ;;
		esac
		#touch $RFSDIR//run/dbus/system_bus_socket
        show_one_line $sudopt chroot $RFSDIR DBUS_SESSION_BUS_ADDRESS=unix:path=/dev/null apt-get install --no-install-recommends -y $PKGS

		# set U-Boot version info in userland to be readable in linux runtime
		[ -d $FBOUTDIR/bsp/u-boot ] && uvfile=$(find $FBOUTDIR/bsp/u-boot/*/output/*/include/generated -name version_autogenerated.h | head -1)
		if [ -n "$uvfile" -a -f $uvfile ]; then
			ubootVersion=`head -1 $uvfile | cut -d' ' -f3`
			ubootVersion=`echo $ubootVersion | sed 's/\"//g'`
			echo -e "Bootloader Version: U-Boot $ubootVersion" | $sudopt tee -a $RFSDIR/etc/buildinfo 1>/dev/null
		fi

		# clean cached packages
		log_mute $sudopt chroot $RFSDIR apt clean
		log_mute $sudopt chroot $RFSDIR apt autoclean
		log_mute $sudopt chroot $RFSDIR apt autoremove -y
    fi

    # fix absolute path for libz.so symlink when cross compiling apitrace
    $sudopt ln -sf libz.so.1 $RFSDIR/usr/lib/aarch64-linux-gnu/libz.so

    # fixup for libtinfo.so needed by tsntool
    $sudopt ln -sf libtinfo.so.6 $RFSDIR/usr/lib/aarch64-linux-gnu/libtinfo.so

    # fixup for Qt6 app cross-compiling
    $sudopt ln -sf libQt6Quick.so.6 $RFSDIR/usr/lib/aarch64-linux-gnu/libQt6Quick.so
    $sudopt ln -sf libQt6OpenGL.so.6 $RFSDIR/usr/lib/aarch64-linux-gnu/libQt6OpenGL.so
    $sudopt ln -sf libQt6QmlModels.so.6 $RFSDIR/usr/lib/aarch64-linux-gnu/libQt6QmlModels.so
    $sudopt ln -sf libQt6Qml.so.6 $RFSDIR/usr/lib/aarch64-linux-gnu/libQt6Qml.so

    #echo -e 'export PYTHONPATH=$PYTHONPATH:/usr/lib/python3.13/dist-packages:/usr/lib/python3/dist-packages' | $sudopt tee -a $RFSDIR/etc/profile 1>/dev/null

    # fix ping: socket: Operation not permitted
    $sudopt chmod u+s $RFSDIR/bin/ping

    fbprint_d $RFSDIR
}

#
# config RFS for each board
#
debian_rfs_post_config() {

	echo "Setup ${MACHINE} ..."
	case "${MACHINE}" in
		imx8mm*)
			sed -i "s/.*pam_nologin.*/auth    requisite       pam_nologin.so nullok/" $RFSDIR/etc/pam.d/gdm-password
			sed -e  's/.*AutomaticLoginEnable.*/AutomaticLoginEnable = true/'  -e '0,/user1/s/.*user1.*/AutomaticLogin = debian/'  -i $RFSDIR/etc/gdm3/daemon.conf
			sed -i '$a\[Install]\nWantedBy=graphical.target\n' $RFSDIR/lib/systemd/system/gdm.service
			#rm -rf $RFSDIR/etc/systemd/system/display-manager.service
			ln -sf /lib/systemd/system/gdm.service $RFSDIR/etc/systemd/system/graphical.target.wants/gdm.service
			ln -sf vsidaemon-IMX8MM $RFSDIR/usr/bin/vsidaemon
			/usr/bin/sed -i '/^gbm-format=argb8888/s/^/# /' $RFSDIR/etc/xdg/weston/weston.ini
			/usr/bin/sed -i '/^xwayland=true$/a modules=screen-share.so' $RFSDIR/etc/xdg/weston/weston.ini
			#sudo chroot $RFSDIR patchelf --replace-needed libGL.so.1 libGLESv2.so.2 /lib/aarch64-linux-gnu/libmutter-*.so.0.0.0

			;;

		imx8mp*)
			sed -i "s/.*pam_nologin.*/auth    requisite       pam_nologin.so nullok/" $RFSDIR/etc/pam.d/gdm-password
			sed -e  's/.*AutomaticLoginEnable.*/AutomaticLoginEnable = true/'  -e '0,/user1/s/.*user1.*/AutomaticLogin = debian/'  -i $RFSDIR/etc/gdm3/daemon.conf
			sed -i '$a\[Install]\nWantedBy=graphical.target\n' $RFSDIR/lib/systemd/system/gdm.service
			#rm -rf $RFSDIR/etc/systemd/system/display-manager.service
			ln -sf /lib/systemd/system/gdm.service $RFSDIR/etc/systemd/system/graphical.target.wants/gdm.service
			ln -sf /lib/systemd/system/imx8-isp.service $RFSDIR/etc/systemd/system/multi-user.target.wants/imx8-isp.service
			ln -sf vsidaemon-IMX8MP $RFSDIR/usr/bin/vsidaemon

			#sudo chroot $RFSDIR patchelf --replace-needed libGL.so.1 libGLESv2.so.2 /lib/aarch64-linux-gnu/libmutter-*.so.0.0.0
			;;

		imx91*)
			sed -i '/^use-g2d=true/s/^/# /' $RFSDIR/etc/xdg/weston/weston.ini
			sed -i '/^gbm-format=argb8888/s/^/# /' $RFSDIR/etc/xdg/weston/weston.ini
			sed -i '/gopoint/d' $RFSDIR/etc/xdg/weston/weston.ini
			ln -sf /lib/systemd/system/weston.service $RFSDIR/etc/systemd/system/graphical.target.wants/weston.service
			;;

		imx93*)
			ln -sf libg2d-pxp.so.2.2.0 $RFSDIR/usr/lib/libg2d.so.2
			sed -i '/^xwayland=true$/a modules=screen-share.so' $RFSDIR/etc/xdg/weston/weston.ini
			ln -sf /lib/systemd/system/weston.service $RFSDIR/etc/systemd/system/graphical.target.wants/weston.service
			;;

		ls1028a*)
			sed -i "s/.*pam_nologin.*/auth    requisite       pam_nologin.so nullok/" $RFSDIR/etc/pam.d/gdm-password
			sed -e  's/.*AutomaticLoginEnable.*/AutomaticLoginEnable = true/'  -e '0,/user1/s/.*user1.*/AutomaticLogin = debian/'  -i $RFSDIR/etc/gdm3/daemon.conf
			sed -i '$a\[Install]\nWantedBy=graphical.target\n' $RFSDIR/lib/systemd/system/gdm.service
			#rm -rf $RFSDIR/etc/systemd/system/display-manager.service
			ln -sf /lib/systemd/system/gdm.service $RFSDIR/etc/systemd/system/graphical.target.wants/gdm.service
			;;

		ls1043*|ls1046*)
			ln -sf /lib/systemd/system/fmc.service $RFSDIR/etc/systemd/system/multi-user.target.wants/fmc.service
			;;
		*)
			echo " Error: ${MACHINE} is not supported "
			exit 1
			;;
	esac

	sed -i 's/^SUBSYSTEM/#SUBSYSTEM/' $RFSDIR/usr/lib/udev/rules.d/50-firmware.rules
	echo "${MACHINE}" > $RFSDIR/etc/hostname
	#/usr/bin/hostnamectl set-hostname "${MACHINE}"
	#/usr/sbin/depmod -a
	#/usr/sbin/update-initramfs -u
}
